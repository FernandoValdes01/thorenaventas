# Mocks de transicion

Este directorio se reserva para mover gradualmente datos mock/hardcodeados desde componentes hacia una capa aislada.

- Estado actual: los seeds productivos viven en `src/data/*.js`.
- Objetivo: reemplazar consumo directo por servicios + API cuando backend este estable.
